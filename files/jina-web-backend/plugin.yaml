name: web-jina
version: 1.0.0
description: "Jina Reader web extract — free tier via r.jina.ai (no API key required, with rate-limited quotas). Optional JINA_API_KEY lifts quotas. Extract-only; search is not supported."
author: nekobaimeow
kind: backend
provides_web_providers:
  - jina
